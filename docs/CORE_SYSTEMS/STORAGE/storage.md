# com.hypixel.hytale.storage

This package contains classes for data storage.

## Overview

The `storage` package provides utilities for managing data storage, particularly for indexed data structures.

### Key Classes

*   `IndexedStorageFile`: A file-based indexed storage system.
*   `IndexedStorageFile_v0`: An older version of the `IndexedStorageFile`.
